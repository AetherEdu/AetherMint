apiVersion: v1
kind: Service
metadata:
  name: aethermint-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 3001
      targetPort: http
